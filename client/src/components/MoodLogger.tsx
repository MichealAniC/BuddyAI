/**
 * Re-export of the feature-based MoodLogger widget.
 *
 * The canonical implementation lives in `components/features/mood/` alongside
 * the other mood-related UI components (MoodSelector, MoodChart).
 */
export { MoodLogger } from './features/mood/MoodLogger';
